Orkano {{ .Chart.AppVersion }} is installing.

{{- if .Values.features.unsafe }}

WARNING: unsafe Orkano features are ENABLED:
  {{ join ", " (sortAlpha .Values.features.unsafe) }}

These opt-ins expand the source/build trust surface. Run `orkano doctor`
after installation and disable any feature you do not need.
{{- end }}

Mint the dashboard login token. It is printed once and only its sha256 hash is
stored, so it cannot be recovered later:

  orkano bootstrap-token --kubeconfig <kubeconfig>

If you have not already, run the capability-probing preflight against this
cluster: it is the documented gate for bring-your-own-cluster installs, and
the same probes resurface as `orkano doctor` checks after install:

  orkano preflight --kubeconfig <kubeconfig>

Check the install's health and hardening once components are up:

  orkano doctor --kubeconfig <kubeconfig>
