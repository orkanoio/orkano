/bin/
coverage.out
.DS_Store
hack/spikes/*/out/
# goreleaser's build output at the repo root.
/dist/
# The dashboard SPA build: Vite's output (embedded by -tags webdist builds,
# produced by `make web`) and the npm tree — never committed. Node-free builds
# embed the tracked dashboard/web/placeholder/ page instead.
/dashboard/web/dist/
node_modules/
# Stray dev build of the local-loop helper (the tracked source is hack/local-loop/).
/local-loop

# Internal planning docs and assistant config — private, never committed.
/PRD.md
/PLANNING.md
/TASKS.md
/CLAUDE.md
.claude/
.codex/
