/bin/
coverage.out
.DS_Store
hack/spikes/*/out/
# goreleaser's build output at the repo root (anchored so it does not also hide
# dashboard/web/dist, the embedded SPA tree the dashboard binary serves).
/dist/
# Stray dev build of the local-loop helper (the tracked source is hack/local-loop/).
/local-loop

# Internal planning docs and assistant config — private, never committed.
/PRD.md
/PLANNING.md
/TASKS.md
/CLAUDE.md
.claude/
.codex/
