# Managed by `orkano init`. Kernel parameters required by k3s when
# protect-kernel-defaults is set — the kubelet refuses to start unless these are
# already applied (docs.k3s.io/security/hardening-guide).
vm.panic_on_oom=0
vm.overcommit_memory=1
kernel.panic=10
kernel.panic_on_oops=1
